class MosquittoIssueSolver:

    def __init__(self, configuration):
        self.configuration = configuration

    def fix_access_control_list(self):
        # todo fix

        # fix
        # for topic in topics:
        #   exists = False
        #   for should_topic in should_topics:
        #       if topic == should_topic:
        #            exists = True

        #   if exists:
        #       result.append(topic)
        pass
